_BASE_: /scratch2/pr65/anur0018/tree_classification/configs/maskdino_R50_rainforests.yaml

DATASETS:
  TRAIN: ("combined_inst_rle_f1000_train",)
  TEST:  ("combined_inst_rle_f1000_val",)

MODEL:
  SEM_SEG_HEAD:
    NUM_CLASSES: 13

INPUT:
  IMAGE_SIZE: 640
  MIN_SCALE: 0.4
  MAX_SCALE: 2.0
  FORMAT: "RGB"
  DATASET_MAPPER_NAME: "coco_instance_lsj"
  MASK_FORMAT: "bitmask"

SOLVER:
  IMS_PER_BATCH: 4
  BASE_LR: 0.0001
  MAX_ITER: 50000
  STEPS: (40000, 47000)
  WARMUP_ITERS: 200
  CHECKPOINT_PERIOD: 5000
  AMP:
    ENABLED: True

TEST:
  EVAL_PERIOD: 5000

DATALOADER:
  NUM_WORKERS: 8
  FILTER_EMPTY_ANNOTATIONS: True
